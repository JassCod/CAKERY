// Your Supabase project details (Supabase → Project Settings → API).
// The "anon public" key is designed to be public: the database only allows
// what supabase/setup.sql permits, per signed-in staff role.
// Tip: instead of editing this file you can set the repository variables
// SUPABASE_URL and SUPABASE_ANON_KEY — the GitHub Pages workflow writes them here.
window.CAKERY_CONFIG = {
  supabaseUrl: '',
  supabaseAnonKey: '',
};
