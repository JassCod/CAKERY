{
  "name": "cakery",
  "version": "1.0.0",
  "description": "Cakery — bakery management system (static web app on GitHub Pages + Supabase database).",
  "private": true,
  "scripts": {
    "start": "node scripts/serve.mjs",
    "test": "node --test test/*.test.mjs"
  },
  "license": "UNLICENSED",
  "devDependencies": {
    "@electric-sql/pglite": "^0.5.8"
  }
}
